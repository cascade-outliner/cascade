import * as stylex from "@stylexjs/stylex";

export const rowVars = stylex.defineVars({
	actionsOpacity: "0",
});

export const bulletVars = stylex.defineVars({
	overlayOpacity: "0",
	dotColor: "currentColor",
});
