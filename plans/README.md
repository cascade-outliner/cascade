# Implementation Plans

Generated by the improve skill on 2026-07-20, from a `standard`-effort audit of
commit `4fa856d`. Execute in the order below unless dependencies say otherwise.
Each executor: read the plan fully before starting, honor its STOP conditions,
and update your row when done.

Selection note: this run was non-interactive, so per the skill's default the
top 5 findings by leverage were planned. The full audit surfaced more findings
than these; the rest are recorded below under "Deferred" and "Considered and
rejected" so they aren't re-audited from scratch next run.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Fix stale auth-ownership docs (CLAUDE.md/README/env example) | P1 | S | — | TODO |
| 002 | Honest CI verification: typecheck workflow, unbreak web test scripts | P1 | S | — | TODO |
| 003 | Due-date range filter includes the end day | P1 | S | — | TODO |
| 004 | DB-backed characterization tests for node procedures | P1 | L | — | TODO |
| 005 | Reject mismatched anchor/parent pairs in createNode/moveNode | P2 | S | 004 | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale — finding fixed independently or approach abandoned)

## Dependency notes

- 005 requires 004 because its regression tests live in the DB-backed suite 004 creates, and characterization must exist before behavior changes in `node.procedures.ts`.
- 001–003 are independent of everything and of each other.
- Repo CI reminders for every executor: PR titles are conventional-commit linted; every PR must close a linked issue; PRs touching `apps/app/` must touch `CHANGELOG.md` or carry the `skip-changelog` label (each plan says which applies).

## Deferred (audited, real, not planned this run — candidates for the next batch)

- **`visibleTree` pagination re-walks the whole visible subtree per page** (`node.procedures.ts:86-112`; the recursion has no cursor predicate — O(total visible) per page). Effort L. Do after 004 so the rewrite is characterized; use `e2e-perf/` to measure before/after.
- **Per-row correlated subqueries in `visibleTree`** (`has_children` EXISTS, per-row tag `array_agg`, `lead()` window at `node.procedures.ts:104-107`). Effort M, behavior-preserving; do with/after the pagination rework.
- **Every move refetches the whole visible tree and truncates loaded pages** (`use-move-mutation.ts:50-54` invalidates unconditionally; the base query holds only page 1). Effort S but MED risk on the optimistic layer — safer once mutation-cache tests exist.
- **Depth-64 cap: silent truncation and a theoretical cycle via a >64-deep move** (`node.procedures.ts:102,433,512`). Enforce a real max depth at write time instead of relying on the CTE cap. Effort M.
- **Expand fetches only the first 500 descendants and ignores `nextCursor`** (`use-toggle-mutation.ts:32`). Effort M; pairs with the pagination rework.
- **`due_date` stored as `timestamptz` but modeled as a calendar date** (`node.schema.ts:46`, `z.coerce.date()` inputs). Needs a migration + backfill decision; plan 003 fixes the user-visible symptom first.
- **CSP `script-src 'unsafe-inline'`** (`packages/http/src/security-headers.ts:5`) — replace with a nonce/hash for the theme bootstrap script in `__root.tsx`. Effort M, MED regression risk; needs a careful rollout.
- **No email verification on sign-up** (`packages/auth/src/server.ts:22`) — needs an email transport; a product decision more than a fix. Record an ADR if intentionally deferred.
- **Marketing hero statically bundles Lexical + drag-and-drop** (`apps/web/src/components/marketing/hero.tsx:1` imports `VirtualTree` directly). Code-split or render a read-only demo variant. Effort M.
- **Pure tree helpers in `packages/outliner` (visible-rows.ts) have zero tests and no test script**; optimistic mutation modules likewise (only `use-visible-tree.test.tsx` exists, heavily mocked). Natural follow-up to 004.
- **Client mutation errors bypass the structured logger** (`root-provider.tsx:7-9` `console.error` + the repo's only TODO). Effort S.
- **`kysely` override duplicated** in `pnpm-workspace.yaml` and root `package.json` (workspace file wins); `allowBuilds.better-sqlite3` looks like a stale leftover on a Postgres-only app. Effort S.
- **Direction options** (maintainer's call, grounded in the audit): full-text search over node content (no search procedure exists; `lexicalToPlainText` + a `tsvector` column make it cheap); tree export/import for the self-hosting audience (currently no data egress at all); new node types via the deliberately extensible registry in `packages/outliner/src/node-types.ts` (ships only `text`/`task`).

## Findings considered and rejected

- **Cursor/ORDER BY collation mismatch in `visibleTree`** (reported as a MED-confidence bug): rejected after vetting. The `cursor::text[]` parameter cast contributes no collation, so Postgres's collation derivation resolves the comparison and sort under the `order` column's `COLLATE "C"`. Plan 004's pagination test (case 8) will lock the behavior in; revisit only if that test fails on a locale-default database.
- **Split `node.procedures.ts` (528-line "god module")**: not worth doing now — cohesion is reasonable, and the queued data-layer work would immediately churn any split. Reconsider if the file keeps growing.
- **esbuild moderate advisory (GHSA-67mh-4wv8-2f99)** from `pnpm audit`: dev-server-only, reachable only via `drizzle-kit` tooling, not production runtime. No action.
- **Silent no-op updates (`toggleNodeExpanded`/`setNodeDueDate`/etc. return void even when the id isn't the user's)**: safe by construction (userId predicate); not worth an error-shape change on its own.
