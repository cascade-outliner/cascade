DATABASE_URL=postgres://postgres:postgres@localhost:5432/cascade
# Signs the session cookie. Only this app reads it (apps/website has no auth config of its own).
BETTER_AUTH_SECRET=change-me-to-a-random-string-of-32-plus-chars
# This app's own origin.
BETTER_AUTH_URL=http://localhost:3001
# Production only: set to .cascadelist.com so the session spans subdomains.
# COOKIE_DOMAIN=.cascadelist.com
# This app's own marketing/legal counterpart (apps/website).
VITE_WEB_URL=http://localhost:3000
PORT=3001
# Optional: enables POST /api/maintenance/purge-tree-history. Use a random
# secret of at least 32 characters and send it as a Bearer token.
TREE_HISTORY_PURGE_TOKEN=
# Optional: default retention window (in days) for tree history. Used both
# as the read/restore visibility cutoff and as the purge job's default
# --days value, so they stay in sync. Defaults to 30.
# TREE_HISTORY_RETENTION_DAYS=30
BETTER_AUTH_GOOGLE_CLIENT_ID=
BETTER_AUTH_GOOGLE_CLIENT_SECRET=
